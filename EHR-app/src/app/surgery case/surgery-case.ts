export interface SurgeryCase {
  surgeryCaseId?: number;  // optional because it's generated by backend
  procedure: string;
  startTime: string;
  endTime: string;
  surgeon: string;
  anesthesia: string;
  nurse: string;
  scrub: string;
  patient: string;
  roomId: number;
  surgeryCaseStatus?: string; // optional
}